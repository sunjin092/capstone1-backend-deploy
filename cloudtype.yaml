name: capstone-cv-api
app: python

options:
  build:
    requirementsPath: requirements.txt

  run:
    command: uvicorn main:app --host 0.0.0.0 --port $PORT
    env:
      - name: PORT
        value: 8080
